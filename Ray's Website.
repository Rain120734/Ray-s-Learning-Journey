<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ray | Robotics & Science Portfolio</title>
    <style>
        :root {
            --bg: #0b0f1a;
            --card-bg: #161b22;
            --accent: #58a6ff; /* GitHub Blue */
            --secondary: #238636; /* GitHub Green */
            --text-main: #c9d1d9;
            --text-bright: #ffffff;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
            background-color: var(--bg);
            color: var(--text-main);
            margin: 0;
            line-height: 1.6;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            padding: 60px 0;
            text-align: center;
            border-bottom: 1px solid #30363d;
        }

        h1 { font-size: 2.5rem; color: var(--text-bright); margin-bottom: 10px; }
        .subtitle { font-size: 1.2rem; color: var(--accent); font-weight: 300; }

        /* Skills Section */
        .skills-bar {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
            margin-top: 20px;
        }

        .skill-tag {
            background: #21262d;
            border: 1px solid #30363d;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9rem;
            color: var(--accent);
        }

        /* Project Cards */
        .section-title {
            margin-top: 50px;
            border-bottom: 1px solid #30363d;
            padding-bottom: 10px;
            color: var(--text-bright);
        }

        .project-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .card {
            background: var(--card-bg);
            border: 1px solid #30363d;
            border-radius: 6px;
            padding: 20px;
            transition: 0.2s;
        }

        .card:hover {
            border-color: #8b949e;
            transform: translateY(-3px);
        }

        .card h3 { margin-top: 0; color: var(--accent); }
        
        .repo-link {
            display: inline-block;
            margin-top: 15px;
            color: var(--secondary);
            text-decoration: none;
            font-weight: bold;
            font-size: 0.9rem;
        }

        footer {
            text-align: center;
            margin-top: 80px;
            padding: 40px;
            font-size: 0.8rem;
            border-top: 1px solid #30363d;
        }

        a { color: var(--accent); text-decoration: none; }
        a:hover { text-decoration: underline; }
    </style>
</head>
<body>

<div class="container">
    <header>
        <h1>Ray's Robotics Lab</h1>
        <p class="subtitle">Building the future, one circuit at a time.</p>
        
        <div class="skills-bar">
            <span class="skill-tag">Python</span>
            <span class="skill-tag">ROS 2</span>
            <span class="skill-tag">Arduino</span>
            <span class="skill-tag">CAD/3D Printing</span>
            <span class="skill-tag">C++</span>
        </div>
    </header>

    <h2 class="section-title">Projects</h2>
    <div class="project-grid">
        <div class="card">
            <h3>🤖 LiDAR Rover</h3>
            <p>An autonomous robot that maps rooms using SLAM algorithms and obstacle avoidance.</p>
            <a href="#" class="repo-link">View Code →</a>
        </div>

        <div class="card">
            <h3>🧪 Physics Engine</h3>
            <p>A custom-built Python engine to simulate fluid dynamics and gravity in 2D space.</p>
            <a href="#" class="repo-link">View Code →</a>
        </div>

        <div class="card">
            <h3>🦾 Bionic Hand</h3>
            <p>Low-cost 3D printed prosthetic with servo motors controlled by flex sensors.</p>
            <a href="#" class="repo-link">View Code →</a>
        </div>
    </div>

    <h2 class="section-title">Contact & Socials</h2>
    <p>I'm always open to collaborating on open-source science and robotics projects!</p>
    <ul>
        <li>GitHub: <a href="https://github.com/yourusername">@yourusername</a></li>
        <li>LinkedIn: <a href="#">Ray [Your Last Name]</a></li>
        <li>Email: <a href="mailto:ray@example.com">Contact Me</a></li>
    </ul>

    <footer>
        &copy; 2024 Ray | Hosted on GitHub Pages
    </footer>
</div>

</body>
</html>
